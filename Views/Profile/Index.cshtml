@model unibucGram.Models.User

@{
    ViewData["Title"] = "Profile";
    var posts = ViewBag.Posts as List<unibucGram.Models.Post> ?? new List<unibucGram.Models.Post>();
    var followers = (int)(ViewBag.FollowersCount ?? 0);
    var following = (int)(ViewBag.FollowingCount ?? 0);
    var isOwnProfile = (bool)(ViewBag.IsOwnProfile ?? false);
    var canViewPosts = (bool?)(ViewBag.CanViewPosts) ?? true; // ADDED
}

<div class="container mt-4" style="max-width: 1140px;">
    <!-- Profile Header -->
    <div class="card mb-4">
        <div class="card-body p-4">
            <div class="row align-items-center">
                <div class="col-auto">
                    <img src="@(string.IsNullOrWhiteSpace(Model.PfpURL) ? "/uploads/default_pfp.jpg" : Model.PfpURL)" 
                         alt="Profile Picture" 
                         width="100" 
                         height="100" 
                         class="rounded-circle border border-3 border-purple" />
                </div>
                <div class="col">
                    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4">
                        <div>
                            <h3 class="mb-1">@Model.FirstName @Model.LastName</h3>
                            <p class="text-muted mb-0">@@@Model.UserName</p>
                        </div>
                        <div class="mt-3 mt-md-0">
                            @if (!isOwnProfile)
                            {
                                var isFollowedByCurrentUser = ViewBag.IsFollowedByCurrentUser as bool? ?? false;
                                var followRequestSent = ViewBag.FollowRequestSent as bool? ?? false;
                                if (isFollowedByCurrentUser)
                                {
                                    <form method="post" asp-controller="Profile" asp-action="FollowToggle" asp-route-userId="@Model.Id" class="d-inline">
                                        <button type="submit" class="btn btn-sm btn-primary btn-follow-toggle">Unfollow</button>
                                    </form>
                                }
                                else if (followRequestSent)
                                {
                                    <button type="button" class="btn btn-sm btn-outline-primary" disabled>Requested</button>
                                }
                                else
                                {
                                    <form method="post" asp-controller="Profile" asp-action="FollowToggle" asp-route-userId="@Model.Id" class="d-inline">
                                        <button type="submit" class="btn btn-sm btn-primary btn-follow-toggle">Follow</button>
                                    </form>
                                }
                            }
                            else
                            {
                                <a asp-area="Identity" asp-page="/Account/Manage/Index" class="btn btn-sm btn-primary">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" class="bi bi-pencil me-1" viewBox="0 0 16 16">
                                        <path d="M12.146.146a.5.5 0 0 1 .708 0l3 3a.5.5 0 0 1 0 .708l-10 10a.5.5 0 0 1-.168.11l-5 2a.5.5 0 0 1-.65-.65l2-5a.5.5 0 0 1 .11-.168zM11.207 2.5 13.5 4.793 14.793 3.5 12.5 1.207zm1.586 3L10.5 3.207 4 9.707V10h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.293zm-9.761 5.175-.106.106-1.528 3.821 3.821-1.528.106-.106A.5.5 0 0 1 5 12.5V12h-.5a.5.5 0 0 1-.5-.5V11h-.5a.5.5 0 0 1-.468-.325"/>
                                    </svg>
                                    Edit Profile
                                </a>
                            }
                        </div>
                    </div>

                    @if (!string.IsNullOrWhiteSpace(Model.Bio))
                    {
                        <p class="mb-3 text-secondary">@Model.Bio</p>
                    }

                    <div class="d-flex gap-4">
                        <div>
                            <strong class="text-purple">@posts.Count</strong>
                            <span class="text-muted">posts</span>
                        </div>
                        <div style="cursor: pointer;" class="followers-link" data-username="@Model.UserName">
                            <strong class="text-purple">@followers</strong>
                            <span class="text-muted">followers</span>
                        </div>
                        <div style="cursor: pointer;" class="following-link" data-username="@Model.UserName">
                            <strong class="text-purple">@following</strong>
                            <span class="text-muted">following</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Posts Section -->
    <div class="card">
        <div class="card-header bg-white border-bottom">
            <h5 class="mb-0 text-purple">Posts</h5>
        </div>
        <div class="card-body p-3">
            @* ADDED PRIVACY CHECK *@
            @if (!canViewPosts && !isOwnProfile)
            {
                <div class="text-center py-5">
                    <i class="bi bi-lock-fill fs-1 text-muted mb-3"></i>
                    <h5 class="mb-2">This Account is Private</h5>
                    <p class="text-muted mb-0">Follow this account to see their posts.</p>
                </div>
            }
            else
            {
                @* ORIGINAL POSTS LOGIC *@
                if (!posts.Any())
                {
                    <div class="text-center py-5">
                        <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-images text-muted mb-3" viewBox="0 0 16 16">
                            <path d="M4.502 9a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3"/>
                            <path d="M14.002 13a2 2 0 0 1-2 2h-10a2 2 0 0 1-2-2V5A2 2 0 0 1 2.002 3h10a2 2 0 0 1 2 2zm-1 0V6l-2.646-2.354a.5.5 0 0 0-.63-.062L7.002 7 4.914 5.584a.5.5 0 0 0-.566-.032L1.002 7V13a1 1 0 0 0 1 1h10a1 1 0 0 0 1-1"/>
                        </svg>
                        <p class="text-muted">@(isOwnProfile ? "You haven't" : "This user hasn't") posted anything yet.</p>
                        @if (isOwnProfile)
                        {
                            <a asp-controller="Posts" asp-action="New" class="btn btn-primary mt-2">Create Your First Post</a>
                        }
                    </div>
                }
                else
                {
                    <div class="row row-cols-3 g-3">
                        @foreach (var post in posts)
                        {
                            var noMedia = string.IsNullOrEmpty(post.ImageURL) && string.IsNullOrEmpty(post.VideoURL);
                            var hasVideo = !string.IsNullOrEmpty(post.VideoURL);
                            <div class="col">
                                <a href="#"
                                   class="text-decoration-none post-thumbnail @(noMedia ? "text-only-thumb" : "")"
                                   data-bs-toggle="modal"
                                   data-bs-target="#postModal"
                                   data-post-id="@post.Id">
                                    <div class="card h-100 border-0 shadow-sm @(noMedia ? "text-only-card" : "")" id="post-@post.Id">
                                        @if (hasVideo)
                                        {
                                            <div class="post-image-container video-thumbnail-container" style="position: relative; background: #000; height: 250px; overflow: hidden;" data-video-url="@post.VideoURL">
                                                <video class="w-100 h-100 video-thumbnail" style="object-fit: cover; display: block; height: 250px;" muted playsinline preload="auto">
                                                    <source src="@post.VideoURL" type="video/mp4">
                                                </video>
                                                <div style="position: absolute; top: 0; left: 0; right: 0; bottom: 0; display: flex; align-items: center; justify-content: center; background: rgba(0,0,0,0.3); pointer-events: none;">
                                                    <div style="background: rgba(255,255,255,0.9); border-radius: 50%; width: 70px; height: 70px; display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 15px rgba(0,0,0,0.3);">
                                                        <i class="bi bi-play-fill" style="font-size: 2.5rem; color: #8b5cf6; margin-left: 5px;"></i>
                                                    </div>
                                                </div>
                                            </div>
                                        }
                                        else if (!string.IsNullOrEmpty(post.ImageURL))
                                        {
                                            <div class="post-image-container">
                                                <div class="post-image" style="background-image: url('@post.ImageURL');"></div>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="p-3 text-only-preview d-flex flex-column justify-content-center align-items-center h-100">
                                                <div class="text-only-icon mb-2">
                                                    <i class="bi bi-chat-square-text fs-3 text-purple"></i>
                                                </div>
                                                @if (!string.IsNullOrEmpty(post.Content))
                                                {
                                                    <p class="small mb-0 text-center fw-semibold line-clamp-4">@post.Content</p>
                                                }
                                                else
                                                {
                                                    <p class="small mb-0 fst-italic text-muted">No caption</p>
                                                }
                                            </div>
                                        }
                                        <div class="card-body p-2">
                                            @if (!string.IsNullOrEmpty(post.Content))
                                            {
                                                <p class="card-text small text-truncate mb-2" title="@post.Content">@post.Content</p>
                                            }
                                            <div class="d-flex justify-content-between align-items-center">
                                                <small class="text-muted" style="font-size: 0.7rem;">@post.CreatedAt.ToLocalTime().ToString("M/d/yy")</small>
                                                <div>
                                                    <span class="badge bg-light text-dark likes-count" style="font-size: 0.65rem;">
                                                        <i class="bi bi-heart-fill text-danger"></i> @post.Likes.Count
                                                    </span>
                                                    <span class="badge bg-light text-dark comments-count ms-1" style="font-size: 0.65rem;">
                                                        <i class="bi bi-chat-fill"></i> @post.Comments.Count
                                                    </span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        }
                    </div>
                }
            }
        </div>
    </div>
</div>

<!-- Post Modal -->
<div class="modal fade" id="postModal" tabindex="-1" aria-labelledby="postModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-centered">
    <div class="modal-content" style="background: transparent; border: none;">
      <div class="modal-body" id="postModalBody">
        <!-- Post content will be loaded here via AJAX -->
      </div>
    </div>
  </div>
</div>

<!-- Followers/Following Modal -->
<div class="modal fade" id="followModal" tabindex="-1" aria-labelledby="followModalLabel" aria-hidden="true" data-own-profile="@isOwnProfile.ToString().ToLower()">
  <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="followModalLabel">Followers</h5>
        <button type="button" class="btn-close" id="CloseFollowModal" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body" id="followModalBody" style="max-height: 400px; overflow-y: auto;">
        <div class="text-center py-3">
          <div class="spinner-border text-purple" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>


@section Scripts {
<style></style>
}
